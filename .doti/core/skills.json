{
  "schemaVersion": 1,
  "maturity": "command-aware-advisory",
  "commandTemplateDir": ".doti/core/templates/commands",
  "agentContextRef": ".doti/agent-context.md",
  "introTemplate": "Read `{agentContextRef}`, then follow `{commandTemplate}`.",
  "operatorQuestionProtocol": "## Asking the operator a question (required format)\n\nAny operator-facing question — at any stage, for Claude or Codex — uses this fixed format, presented immediately before the question:\n\n- **Context** — the full background. Assume the operator has not re-read the material; restate what is unclear and the relevant facts.\n- **Why it matters** — the concrete impact and why it needs a decision now.\n- **Options** — for each option: **Pros**, **Cons**, and **Consequence** (what becomes true downstream if chosen).\n- **Recommendation** — the option you recommend and the reasoning; list it first and label it \"(Recommended)\".\n- **Assumptions** — the assumptions behind the recommendation.\n- **Confidence** — High / Medium / Low, with a one-line reason.\n\nEvidence requirement: every question, option, recommendation, and assumption must rest on verified facts. Verify each premise first (read the code, run the tool, observe the output) and cite that evidence; never present options whose premises you have not confirmed. An assumption is allowed only when the fact cannot be obtained from this environment — then label it UNVERIFIED, state why it cannot be verified and what would verify it. Prove claims (reproduce/RCA); never assert them. Do not use \"assume\" to defer work you could do — an unproven premise is a defect that can ruin the design.",
  "skills": [
    {
      "name": "doti-specify",
      "description": "Create or refine a scaffold-dotnet specification in command-aware-advisory mode.",
      "argumentHint": "[feature or goal]",
      "highlights": [],
      "nextStage": "Run `/02-doti-clarify` to resolve ambiguities, or `/03-doti-plan` when no clarification is needed."
    },
    {
      "name": "doti-clarify",
      "description": "Clarify scaffold-dotnet requirements while preserving command availability boundaries.",
      "argumentHint": "[question or request]",
      "highlights": [
        "Clarify specifics: ask blocking questions one at a time via the `AskUserQuestion` tool, fold each answer into the spec's `## Clarifications` before asking the next, and let the operator stop at any point. Use the shared operator-question format above (Context / Why it matters / Options / Recommendation / Assumptions / Confidence + the evidence requirement) for every question."
      ],
      "nextStage": "Run `/03-doti-plan` to author the implementation plan."
    },
    {
      "name": "doti-plan",
      "description": "Plan scaffold-dotnet work with phase, gate, and advisory-gap awareness.",
      "argumentHint": "[implementation goal]",
      "highlights": [
        "This stage ORIGINATES the best design (arch-review later challenges it; implement preserves it). Two rules: (1) Assess the existing architecture BEFORE deciding — read the similar existing code, the *.Core vs *.Cli boundary the change lands in, the command->CliResult->error-code convention, rules/architecture.json + .sentrux/rules.toml, and any scaffold/templates/** implications; the design must fit those patterns or justifiably change them (name which). (2) Choose the SIMPLEST CORRECT, MODULAR design — effort is NOT a design criterion: never pick an approach because it is less work. Prefer the simplest design that satisfies the FR/SCs, fits the existing patterns, preserves deterministic proof (gates stay fail-closed, nothing downgraded enforced->advisory), and avoids future drift; and make it modular — small single-responsibility units, a named *.Core type per behavior, one concern per file/class, composed rather than INLINED (right-sized, not premature abstraction). Reject alternatives by architectural trade-off, not by convenience. Record Decision/Rationale/Alternatives-rejected and the architecture delta (the ArchUnit family + Sentrux boundary) so the design is enforced, not just described."
      ],
      "nextStage": "Run `/04-doti-tasks` to break the plan into executable tasks."
    },
    {
      "name": "doti-tasks",
      "description": "Convert scaffold-dotnet plans into executable tasks without hiding missing gates.",
      "argumentHint": "[plan or scope]",
      "highlights": [],
      "nextStage": "Run `/05-doti-analyze` for a cross-artifact consistency review."
    },
    {
      "name": "doti-analyze",
      "description": "Analyze scaffold-dotnet source, installed assets, docs, and advisory gaps.",
      "argumentHint": "[files or topic]",
      "highlights": [],
      "nextStage": "Run `/06-doti-arch-review` for a multi-lens design review of the spec against the current code."
    },
    {
      "name": "doti-arch-review",
      "description": "Review scaffold-dotnet design via a multi-lens panel — only the lenses the change touches — against the spec + current code (not a Sentrux/ArchUnitNET pass).",
      "argumentHint": "[architecture change]",
      "highlights": [
        "A multi-lens DESIGN review before implementation. The stage ALWAYS runs (a hard prereq of /07) but its DEPTH is conditional (FR-028/SC-013): TRIAGE the change first, then activate ONLY the lenses it actually touches — a lens whose concern the change does not touch EXITS 'not applicable' and spawns no sub-agent. CRUCIAL triage split: 'templates' is not one thing — Doti PROSE (command templates under .doti/core/templates/commands/, skills, agent-context, docs) is no-code and skips the code lenses; but GENERATED-CODE templates under `scaffold/templates/**` (the .cs/.csproj/tests/rules that become a real generated repo) ARE code — run the code lenses there exactly like production ('it's a template' is no excuse). Run the applicable lenses (design-soundness, edge-case/failure-mode, data-contract, security, blast-radius, simpler-alternative, testability, fit-with-current-architecture) — each carries a pre-canned 'look for' checklist; if you are capable of sub-agents, run one clean-context sub-agent per lens IN PARALLEL — against the spec/plan/tasks + current code, with the changed-files context fetched ONCE (`hx impact plan --for arch-review --json`, or `git diff` fallback) and injected into every lens; each finding carries severity (BLOCKER/HIGH/MEDIUM/LOW) + evidence (`file:line` or spec/plan ref) + a fix. ALWAYS write the review to `docs/reviews/{NNN-slug}-arch-review.md` — the review record /08-drift-review reads (a future cycle binds it as the stage's produces artifact); a no-code change still writes a minimal no-op record naming the triage result, it does not skip the stage. Sentrux/ArchUnitNET are NOT run here — they measure implemented code and are enforced by `gate run` at `/07` (the `cliSurfaceConfinement`/`cliDelegation` ArchUnitNET families + the Sentrux layer/cycle boundaries). An open BLOCKER in an applicable lens, or a missing applicable lens, blocks `/07-doti-implement`; a correctly-skipped lens does not."
      ],
      "nextStage": "Run `/07-doti-implement` to implement the tasks."
    },
    {
      "name": "doti-implement",
      "description": "Implement scoped scaffold-dotnet tasks with advisory-gap handoff.",
      "argumentHint": "[task]",
      "highlights": [
        "Implement REINFORCES the approved design — it is NOT where design happens. Make narrow edits aligned with the spec/plan/tasks, built MODULARLY — extract each distinct concern into a named, separately-testable method/type (logic in a *.Core type; the CLI stays parse->delegate->render), composed rather than INLINED; keep methods within the Sentrux function-size limit (don't over-split either). If implementation reveals the plan/arch-review is materially wrong (a better architecture, or a design that won't satisfy an FR/SC), STOP — do not silently code a different design (drift /08 + the gate can't fully catch) and do not force-fit a plan you've found wrong. Report the mismatch and re-run /03-doti-plan (then /04-doti-tasks / /06-doti-arch-review as needed) before coding through it.",
        "Engineering discipline (required): RCA root causes — don't patch symptoms; validate every assumption by reading the code, running the command, or observing the output (never assume; prove claims with evidence); take no shortcuts (don't silence checks, hard-code around problems, or declare done without proof); hold a 95%-confidence bar — if not ≥95% confident the solution and code are correct, keep finding better approaches or validating until you are; if truly blocked, surface it with what you tried rather than guess; report honestly (what was validated, what remains, deferrals with rationale)."
      ],
      "nextStage": "Run `/08-doti-drift-review` to check the diff against the approved design."
    },
    {
      "name": "doti-drift-review",
      "description": "The last consistency gate before release: detect drift across the spec, the code, the docs, and the installed/rendered assets.",
      "argumentHint": "[optional scope]",
      "highlights": [
        "SCOPE FROM THE IMPLEMENTATION DIFF first, don't scan blind — resolve the cycle base (`baseRef` in `.doti/cycle-state.json`, the commit BEFORE implementation) and take `git diff <baseRef>..HEAD` plus the working tree: the before→after change set. Every axis works from what CHANGED this cycle, not the whole repo. (Agent-run `git diff` for now; a future cycle folds the diff into hx as a first-class command, like the T040 arch-review changed-files context.) Then THREE drift axes, not just asset parity — a green gate proves the code is internally sound; drift-review proves it still AGREES with everything around it. (1) spec↔code: each changed hunk satisfies its FR/SC with a real enforcing mechanism and matches the approved arch-review/plan design — nothing silently downgraded from enforced to advisory, no logic that belongs in *.Core landing in *.Cli; back it with `hx doti converge` for the (not-diff-bound) spec→tasks coverage gap, then spot-check the covering code. (2) code↔docs: for each CODE change in the diff, the doc that describes it (README/CHANGELOG/packaging + the agent context CLAUDE.md/AGENTS.md/.doti/agent-context.md + each rendered skill's Command-availability + `hx describe`/`--help`) must have changed to match — a code change with NO doc change is the prime suspect; and for each symbol the diff REMOVED/renamed, grep the repo so the old name survives in no doc (the stale-Velopack class of miss). (3) source↔installed: run BOTH parity authorities — `doti render-skills --check` (rendered skills/agent-context/entrypoints) AND `doti payload check --repo .` (the full installed `.doti` payload, static assets + rendered) — plus thin AGENTS.md/CLAUDE.md entrypoints, no hand-edited installed skills (fix the `.doti/core` source + re-render). Then `gate run --profile normal` (blocking) and stamp drift-review ONLY on a clean review — the stamp is fail-closed (a clean stamp is the readiness proof; there is no `commit` stage, the coded transition into /09 owns the commit) — open drift in any applicable axis blocks the stamp. Triage the diff first (a docs-only diff skips the spec↔code design checks); if you can spawn sub-agents, run each applicable axis as a parallel pass over the diff."
      ],
      "nextStage": "Run `/09-doti-release` to release, or `/01-doti-specify` to add another feature to this release train."
    },
    {
      "name": "doti-release",
      "description": "Run scaffold-dotnet release proof and command-backed local release output.",
      "argumentHint": "[release intent]",
      "highlights": [
        "Default version intent is cycle-type-aware (FR-044/SC-016): a feature cycle defaults to `minor` — the release-stage transition (`doti cycle stamp --stage release`) writes `+semver: minor` into the automatic transition commit, so GitVersion calculates a minor bump and `hx release`'s blank intent (which follows that bump) validates by default rather than failing 'Release intent mismatch'. A bug-fix-only cycle (the FR-034 assess->fix->test mini-cycle) never hits that transition, so it writes no minor signal and defaults to `patch`. An explicit `--major|--minor|--patch` overrides the +semver trailer and the release default together (kept in lockstep)."
      ],
      "nextStage": "Cycle complete. Start the next feature with `/01-doti-specify`."
    },
    {
      "name": "doti-upgrade",
      "description": "Upgrade the installed hx tool and reconcile this repo's Doti assets in one operator action (utility — runs anytime, outside the cycle).",
      "argumentHint": "[optional]",
      "highlights": [
        "Drives BOTH update planes. Plane A updates the hx tool on this machine — `dotnet tool update -g Heurex.SpeckitDoti` for the NuGet global-tool channel, or the Microsoft Store for an MSIX install. Plane B reconciles THIS repo's `.doti` via `hx doti install --repo .`: the version-aware reconciliation preserves operator-modified managed files (a modified asset is kept and the bundled version is staged as a `.new` sidecar; an operator-deleted asset is not resurrected without `--force`; a repo whose payload is ahead of the bundled one is refused). Report the tool vX->vY transition (or 'already current') and the repo install/migrate/preserve/block result, flagging any `.new` sidecars to merge."
      ],
      "nextStage": "Resume your active cycle stage, or start a feature with `/01-doti-specify`."
    },
    {
      "name": "doti-bug",
      "description": "Run a bug fix as an enforced mini-cycle: assess (read-only) -> fix (bound to the assessment) -> test (honest). Utility — runs anytime, outside the feature cycle.",
      "argumentHint": "[bug summary or id]",
      "highlights": [
        "ENFORCED assess -> fix -> test via `hx doti bug assess|fix|test`, recorded under `.doti/bugs/<bugId>/`. assess is read-only (verdict/severity/remediation contract; writes no code); fix is the only writer and fails closed unless bound to a CONFIRMED assessment (`bug-assessment-missing` / `bug-fix-unbound`); test is an honest verification where a `pass` requires evidence (an evidence-free pass is downgraded — no over-claiming). Each stage is proof-bound: the fix binds to the assessment's content hash, the test to the fix's. Per-stage detail: `extensions/bug/commands/speckit.bug.{assess,fix,test}.md`."
      ],
      "nextStage": "A passing test closes the bug. Resume your active cycle stage, or start a feature with `/01-doti-specify`."
    },
    {
      "name": "converge",
      "description": "Brownfield/drift reconciliation: assess the codebase vs spec/plan/tasks and append the remaining unbuilt work as new tasks. Utility — runs anytime the ledger has fallen behind.",
      "argumentHint": "[feature slug]",
      "highlights": [
        "Find the requirement coverage gap deterministically with `hx doti converge --spec docs/specs/<NNN-slug>.md --tasks docs/tasks/<NNN-slug>-tasks.md` — it reports every FR-###/SC-### the spec defines that NO task covers (fails closed `converge-input` if a file is missing). For each uncovered requirement, ASSESS it against the codebase (genuinely unbuilt vs already-built-but-unmapped — read/reproduce, don't assume), then append the genuinely-missing work as new tasks in the right MVP-first phase, mapped `[covers FR-/SC-]`. converge never rewrites the ledger for you; you append and the ordered-task gate keeps the order honest. Ties to `/08-doti-drift-review` (spec->tasks gap vs source->installed gap)."
      ],
      "nextStage": "Continue with `/07-doti-implement` to build the appended tasks, or `/08-doti-drift-review` to reconcile installed assets."
    }
  ]
}
