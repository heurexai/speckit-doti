# Changelog

All notable changes to speckit-doti are documented here. The format follows
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/); the project aims to follow
[Semantic Versioning](https://semver.org/spec/v2.0.0.html) (pre-1.0: minor versions may include breaking changes).

## [Unreleased]

- **Fixed** `032-doti-update-worktree-leak` (bug mini-cycle): two coupled defects in `hx doti update`. (1) **Worktree-leak / commit-race / silent swallow** — the self-commit (`DotiReconcileCommit`) ran while the reconcile's own temp worktree was still registered (`DotiWorktreeUpdate` disposed it in `finally`, *after* the commit), so on a worktree-cluttered repo the commit lost a shared-`.git` lock race, `git commit` failed, and the failure was reported as `DotiCommitStatus.Failed` but **swallowed under `ok:true`/exit-0** (changes left staged). Fixed: the worktree is now fully disposed *before* the commit (extracted `RunWorktreeScoped`); a `Commit.Status == Failed` routes to `CliResults.Fail` (`ok:false`/non-zero/`Integrity`) for both `update` and `update-all`; a new **ownership-aware** `GitWorktree.PruneLeakedTemps` reclaims leaked `hx-doti-worktree-*` temps at run start + in `Dispose` (`git worktree remove --force`, else `rm` the corrupted husk, then `git worktree prune`, with a 10-min age floor so a **concurrent** run's live worktree is never killed — a real bug caught during the fix); and the commit retries on a transient `index.lock`/`cannot lock ref` signature before reporting `Failed`. (2) **Vendored-tool reconcile gap** — `tools/{sentrux,gitleaks,gitversion}` are scanned into `managed-assets.json` but the reconcile + parity loops iterate a hard-coded `.doti/{sub}` list that excludes `tools/`, so a doti release that bumps the pinned Sentrux (v0.5.11→v0.5.12) left consumer repos on the old tool with a silently-broken gate (`"Could not read the Sentrux gate baseline signal"`). Fixed: a parallel `tools/{sub}` reconcile pass in `DotiInstaller` (reusing the existing preserve/`.new` machinery; `bin/` excluded so the gitignored exe is never touched) + a `CheckToolFiles` parity check (kind `vendored-tool`) so `payload check` flags a stale manifest/grammar + a `DotiUpdater` advisory to run `hx tools fetch`; the exe stays offline-fetched (`SentruxManifestValidator.Verify` already fail-closes on a hash mismatch → a clear stale-binary error). 16 new deterministic tests; recorded under `.doti/bugs/032-doti-update-worktree-leak/`.
- **Fixed** `033-bug-only-release-path` (bug mini-cycle): a confirmed bug-fix-only mini-cycle (passing `.doti/bugs/<id>/test.json`, no numbered feature cycle, so no `.doti/cycle-state.json`) could not be released as a patch — `CycleService.GetReleaseTrain` (used by `gate run --profile release` and `hx release`) threw *"No cycle state … complete a Doti cycle before release"* on a null store read, **before** the cycle-030 bug-release bridge built to handle exactly this could run (its bug-half never needed the state). Fixed: `BuildReleaseTrain`'s `state` is now `CycleState?` — a null cycle-state yields an empty feature-half while the bug-half (`_bugReleaseMembers`) is still consulted, so a bug-only train with ≥1 confirmed member validates and a truly-empty repo still fails closed; `MarkReleaseTrainReleased` short-circuits before any write (no synthetic state — `BugReleaseGit` self-maintains released-status via git-tag-reachability). The stamp guard was left untouched (verified the release path never calls `CycleService.Stamp`; a bug-only release needs no feature stamp). 6 new `BugOnlyReleasePathTests`; the 4 existing release-train facts unchanged. Recorded under `.doti/bugs/033-bug-only-release-path/`.
- **Added** `031-doti-update-self-contained`: `hx doti install` / `update` / `update-all` are now **self-contained** — one command from any directory reconciles the target repo's managed Doti assets, prunes orphans, and commits, with no manual workarounds. (1) **Bundled-source default** — a new `BundledPayloadResolver` resolves the source to the **running tool's bundled payload** beside the executable (the global-tool's payload root), falling back to the working-directory `FindDotiSource` walk only for a genuine dev/in-repo source, and **failing closed** (`Validation_DotiPayloadSourceUnresolved`) when neither resolves — so the commands no longer require a `cd` into the tool's bundled-payload dir, and the false `already-current` is resolved by construction (the bundled `payload.manifest.json` stamps the real version → honest before≠after). (2) **Complete reconcile** — `DotiInstaller.PruneOneOrphanDir`'s no-baseline branch now **prunes** a `*doti-*` rendered-skill orphan the render no longer targets (rendered skills are not operator content; hand-edits are drift) instead of preserving it, so a cross-version skill rename (e.g. the 026 reorder) leaves no husk and `doti payload check` passes; operator-**policy** assets (`skills.json` under `managed-replace-preserve-live-config`, the constitution) stay preserved. `StageSidecar` skips the `.new` when the bundled content is byte-identical to the operator's file (no spurious stray), and the staged `.new` sidecars are surfaced as a distinct **merge-pending** list. (3) **Self-owned commit** — a new `DotiReconcileCommit` makes, after a successful reconcile in a Git work tree, **one sanctioned commit of exactly the touched managed-asset paths** (`Installed ∪ Removed ∪ Rendered`, minus `.new` + gitignored runtime state — never `git add -A`), setting `DOTI_SANCTIONED_COMMIT=1` on the child `git commit` so the insurance hook permits it, with an auto message naming the before→after move + pruned orphans; idempotent (no staged change → no commit), non-git skips it, and a new `--no-commit` flag opts out. The resolved-source origin, pruned paths, `.new` merge-pending list, and commit outcome (sha / skip reason) are reported in the `--json` envelope + human summary. (4) **Bug-cycle RCA discipline** (`/doti-bug` skill + `speckit.bug.{assess,fix,test}` templates): assess now requires a proper **reproduce → root-cause → validate** RCA; fix must target the **root cause** and explicitly **forbids** a symptom patch/bandaid/workaround; the agent **does** the root fix and must **not** present a bandaid-vs-root choice or ask which fix to apply (surfacing only a genuine blocker). (5) **Doc-stamp scope consistency** (resolves #42): `CycleService.ValidateTransitionReadiness` excludes the active feature's owned produces docs (`FeatureArtifactScope.OwnedPaths`) from its untracked/unstaged scope guards exactly as `Check()` does, so authoring a stage's produces doc ahead of stamping the transition into it no longer trips the set-aside/stage/restore dance — a genuinely foreign untracked path still blocks. Logic lives in `Hx.Doti.Core`/`Hx.Cycle.Core`; the CLI stays parse→delegate→render; contract changes are additive trailing-optional; no new dependency edge.
- **Added** `029-scaffold-setup-config`: a provenance-tracked setup configuration so a new repo's operator surface is captured once instead of hand-edited across a dozen files. `hx new` and `hx doti install` accept either `--config <doti-setup.json>` (the agent path) or `--interactive` (a lighter human wizard; mutually exclusive). Both are 1:1 over one schema, **validate fail-closed before any file is generated** (an invalid value names its field and leaves no partial repo, SC-006), and persist the operator's intent — repo-portable fields only, never a machine-local absolute path — to a tracked `.doti/setup.json`. The config projects into `GitVersion.yml` (`next-version`), the CLI `.csproj` (`<Description>`/`<RepositoryUrl>`/`<PackageLicenseExpression>`/`<Authors>`, XML-encoded against MSBuild injection), and constitution §2 (write-once-when-placeholder). **`hx doti config show [--json]`** renders the effective config grouped by what each setting drives, marking every key **default vs custom** (the model carries `value`/`source`/`default` per key); with no `.doti/setup.json` it shows the all-default view. Operator-only steps (NuGet Trusted-Publishing OIDC policy, the `NUGET_USER` secret, the `production` Environment, `main` branch protection, the `v*` tag push) and the git/CI setup deferred to `030` are surfaced as an inert **checklist**, never executed. The pure model/resolver/schema/projector live in `Hx.Tooling.Contracts` (writers injected); the `.doti`-asset writers/store/formatter in `Hx.Doti.Core` — no new project edge, no cycle (the design the `/04` arch-review's 2 BLOCKERs forced). Existing no-config `hx new`/`hx doti install` are byte-identical (SC-007).
- **Fixed** `030-bug-release-bridge`: a single completed cycle can now anchor its own release. `CycleService.BuildReleaseTrain` previously counted only completed-unreleased feature cycles plus the active cycle *at* `release`, so a feature parked at `drift-review` (or a bug-fix-only repo) produced an empty, invalid train and `hx release` / `gate run --profile release` refused to proceed. It now also counts the active feature cycle at `drift-review` and UNRELEASED test-passed `/doti-bug` mini-cycles — a bug drops out of the train once its fix commit is reachable from the latest `v*` tag (self-maintaining, no marker to seed). The bug-member provider is injected into `CycleService` as a delegate, preserving the `Hx.Doti.Core → Hx.Cycle.Core` layering (no forbidden edge). Recorded as a `/doti-bug` mini-cycle (assess→fix→test) under `.doti/bugs/030-bug-release-bridge/`.
- **Added** `028-agent-gated-reconcile`: self-describing, agent-gated cycle reconciliation + a code-generated workflow action model. (1) **Agent-gated reviewed-no-impact rebind** — 027 stopped the engine from silently rebinding a content change; this stops the *agent* from rubber-stamping the forced re-run. When an upstream artifact's content changes, a downstream non-review, non-code-bound stage is now a `RestampSafety.ReviewedNoImpact`: the engine surfaces the changed prerequisite paths + the line-level upstream `git diff` (FR-002 — lazily, at the CLI/recovery seam, not the pure evaluator), and the agent must make an explicit IMPACT DECISION — re-author if affected, or `hx doti cycle review-rebind --target <stage> --attest no-impact [--reason]` if not. The eligibility fence (`ReviewRebindEligibility.IsAttestable` = `PrereqArtifactChanged` AND non-review AND `!requireChangeSetIdentity`) is enforced **inside `Stamp`**, so a bare `hx doti cycle stamp` of an attestable stale **refuses** (`Validation_CycleReviewRebindRequiresAttest`) — closing the agent-rubber-stamp hole 027 left open. The verdict is recorded immutably (dependent, changed upstream, before→after hashes, reason) in a new additive `CycleState.ReviewedRebinds` field (one atomic write); it **decays** automatically when the reviewed upstream later moves again; the auto-cascade is **suppressed** so one attestation never silently flips multiple stages Fresh; `OwnArtifactChanged`/`ChangeSetDiffers`/review-kind/change-set-bound stales can never be attested away. (2) **Code-generated workflow action model** — the agent's valid *workflow* next-action commands (stage advance, the recovery menu, the attest verb, the publish-boundary STOP, the train-loop, the bug-phase, the 7 utility skills) are now GENERATED from one `DotiActionModel` registry of `CommandDescriptor`s with **declarative** applicability (a closed named-condition vocabulary, not opaque predicates) over a `CommandContext`, projected onto `CliResult.NextActions`, the recovery menu (**wrapping** the single `CycleRecoveryPlanner` projection — never a second evaluator), the rendered skill refs, and the agent-context `{commandAvailability}` placeholder. It **subsumes `DotiWorkflowRegistry`** (deleted; presentation rehomed in `Hx.Doti.Core` over a new acyclic `Doti.Core → Cycle.Core` edge) and deletes `skills.json nextStage` (utility skills become `Utility` descriptors); payload-derived next-actions (`ImpactCommands`/`Scaffold`) stay locally built. `next:` is declared on all 9 stages + bound in `StageModel` (the `CheckHelpers` DFS de-duplicated onto `StageModel.TransitivePrereqStages`). (3) The §1 **`Self-describing automation`** constitution invariant — the program does the bookkeeping + surfaces the evidence AND the next-action; the agent only judges. Designed through the reordered cycle: a multi-lens `/04-arch-review` caught **7 BLOCKERs** (incl. the bare-`Stamp` fence bypass) + resolved them *before* implement. Full suite green; render parity clean.
- **Fixed** `027-stamp-reconcile-orphan-prune`: codified stamp reconciliation + the `hx doti update` orphan bug. (1) **Freshness cascade** — re-running the one genuinely-changed stage now AUTO-CASCADES a content-equal rebind to its content-unchanged dependents, ending the stage-by-stage hand-re-stamp treadmill. A new `StaleReason.PrereqRebindable` + `RestampSafety.ReBindContentEqual` tier auto-rebinds a stamp ONLY when its own artifact is canonical-unchanged, every producing upstream is `Fresh`, and every shared prerequisite's content is byte-identical (a pure edge/reorder) — a genuine content change, a **review-kind** stage (arch-review/analyze/drift-review), and any **change-set-bound** stage (`ChangeSetDiffers`, the 021/026 lock) ALWAYS stay `RerunRequired` (never rubber-stamped). `cycle refresh --apply-safe` re-stamps both safe tiers (re-deriving per step); `Stamp` auto-cascades the safe rebind (re-entrancy-guarded, isolated so a cascade failure never fails the stamp); an in-flight stage the graph requires but cycle-state never stamped is surfaced as a first-class `inserted-stage` verdict (no more opaque "missing" dead-end); `RebaseProofsToHead` now recomputes `PrerequisiteArtifactHashes` so a transition never re-introduces a false stale. (2) **Update orphan-prune** — `DotiInstaller.Install` (the path `hx doti update`/`update-all` go through) prunes managed skill dirs a payload renamed away, sourced from an **on-disk scan** (so it self-heals repos whose manifest predates the skill category — the agentx 0.15.x renumber husk), deleting only baseline-clean files (operator-edited orphans preserved/blocked) and recording them in `ObsoleteAssets`; `doti payload check` flags a surplus `*-doti-*` dir. New proof fields (`StageGraphFingerprint`) are additive/nullable; the gate-proof digest is byte-unchanged. 24 new tests (incl. the content-change-stays-RerunRequired and the 021/026 ChangeSetDiffers regression locks), full suite green; adversarially verified.
- **Changed** the tasks template (`.doti/core/templates/tasks-template.md`) + the `/doti-tasks` behavior so the **documentation sweep is the permanent, unconditional, FINAL task of every cycle** — update `README.md` (including any stage/command/order lists and tables), `CHANGELOG.md`, the agent context (`CLAUDE.md`/`AGENTS.md`/`.doti/agent-context.md`), each affected rendered skill, and `hx describe`/`--help`, then re-render installed assets. Previously the docs task was a conditional "Polish"-phase placeholder ("update docs when behavior changes"), which is why the `026` slug renumber left the README cycle-order lists stale. **Also fixed** that README drift: the cycle diagram, the stage table, and the Spec Kit comparison row now show `plan → arch-review (04) → tasks (05) → analyze (06)`. Enforced going forward by the `/08` code↔docs axis and the `release-documentation` gate.
- **Changed** `026-arch-review-after-plan`: reordered the doti cycle so the multi-lens **architecture review runs immediately after `plan` and before `tasks`/`analyze`** (was after `analyze`) — the design is reviewed before the coverage/consistency work that depends on it, so an arch-review BLOCKER forces a plan revision before tasks are broken down against a flawed design. New stage order: `specify(01) → clarify(02) → plan(03) → arch-review(04) → tasks(05) → analyze(06) → implement(07) → drift-review(08) → release(09)`; `arch-review` renumbers `06→04`, `tasks` `04→05`, `analyze` `05→06` (slash-command names change accordingly). The stage model is declarative + dual-sourced: the backward prerequisite chain in `.doti/core/workflows/doti/workflow.yml` (`arch-review←plan`, `tasks←arch-review`, `analyze←tasks`, `implement←analyze`) and the ordinal/forward-chain/next-step in `DotiWorkflowRegistry` were changed together (the existing `Workflow_assets_match_registry` test guards their consistency across source **and** installed copies), plus every cross-reference in `skills.json`, the command templates, the agent-context/constitution/spec templates, `README`, and code comments/messages (`ImpactCommands`, `Sentrux*Tracker`/`Policy`, `AffectedPlan`, the `errorcodes` registry → regenerated `ErrorCodes.g.cs`), with the installed skills/agent-context/entrypoints re-rendered (parity clean, 93 files). No stage **IDs**, `produces` patterns, or `cycle-state.json` schema changed, so existing proofs stay interpretable; stage behavior is byte-identical, only repositioned/renumbered. Registry-order tests updated to the new ordinals. **Also fixed** a change-set-identity asymmetry surfaced by this cycle: the stamp and transition-rebase stored the *raw* `base..HEAD` identity while the freshness check subtracted the feature's OWN doc/review artifacts (the 021 fix only reached the check), so a change-set-bound stage (drift-review/release) read a **false "stale"** against its own in-range committed doc. `CycleService` now routes the stamp, the rebase, and the check through one `StageChangeSetIdentity` helper (owned paths always subtracted), with a regression test that fails without the fix.
- **Fixed** `024-release-train-stale-on-unrelated-commit` (bug mini-cycle): the release train re-validated the **active** feature's gate proof by re-planning `BaseRef..HEAD` against the **live** symbolic `HEAD`, so any later unrelated commit (e.g. a separate bug fix on the same branch — `023` landing on top of `022`) moved the endpoint and the UNCHANGED feature read "affected-test proof is stale or forged: planner hash does not match the current change set", blocking the release. A release train is supposed to let new work land without tripping an unchanged, already-implemented prior member. Fix: bound the active feature's re-validation to the feature's OWN latest release-transition commit (`featureReleaseHead` — a `release→release` re-stamp wins over the original `drift-review→release`) rather than live HEAD — `GateProofValidator.ValidateAffectedTestProof`/`ValidateScope` gain a bounded-head parameter (default `null` = live HEAD, preserving the transition path) and `CycleService.ReleaseTrain` passes it. Mirrors the same "never validate against a symbolic ref that re-resolves after HEAD moves" lesson behind the gate-proof base-ref fix. Adds a direct regression test (live-HEAD re-plan drifts; bounded re-plan does not) — the existing docs-only release-train test never reached this path.
- **Fixed** `023-sentrux-gate-untracked` (bug mini-cycle): the Sentrux regression `gate` was blind to **untracked** files, so a feature's brand-new (not-yet-`git add`-ed) files' structural growth escaped the `/07` implement gate and first failed only after they were committed at `/08` — the false-green that complicated the 022 run. RCA'd and validated against the fork source (a sub-agent proved `gate` reads the tracked worktree via `git ls-files` + `fs::read` but skips untracked files; `--include-untracked` existed only on `check`, and `gate --include-untracked` exited 2). Fixed **in the fork** — v0.5.12 wires `--include-untracked` into the `gate` subcommand (the scanner already supported it) — then re-vendored (`tools/sentrux/sentrux.version.json` pins v0.5.12 + the new `gate-include-untracked` required-feature; binary + csharp-grammar SHA-256s verified) and `SentruxProcessAdapter.GateCompare` now passes `--include-untracked`. Dogfood proof: an untracked CC≈60 god-function now drops the quality signal and **fails** the regression gate (was invisible before). Structural growth now surfaces pre-commit at `/07`.
- **Added** `022-doti-repo-version-lifecycle`: first-class doti version-lifecycle commands so an operator can see and move a repo's recorded `.doti/payload.json` version (the gap: `hx doti install`/`payload check` upgrade a repo but never report the version they land on). `hx doti check-version --repo <p>` reports a repo's Doti version + its relation to the installed tool (`current`/`outdated`/`ahead`), distinguishing **not-a-repo** from **version-unknown** rather than silently reporting `unknown`; `hx doti scan --root <d>` recursively tables every Doti-enabled repo under a tree (read-only, error-tolerant, explicit-empty); `hx doti update --repo <p> [--force] [--dry-run]` reconciles one repo's managed assets to the installed payload and reports the **before→after** version, applying the change in an isolated **git worktree** (`--dry-run` previews; git required, fail-closed) and **reusing** the installer's customization-aware reconcile (operator-modified managed assets preserved as `.new` sidecars unless `--force`; operator-owned content like the constitution untouched); and `hx doti update-all --root <d>` batch-updates every repo under a root **fail-soft** with an updated/already-current/failed summary. Also **fixed** `hx version --repo` mis-reporting `targetRelation: newer` for a clean, current doti-adopted repo — it compared the tool against the empty `.doti/scaffold-version.json` (a doti-adopted, non-scaffolded repo has no scaffold version); it now sources the relation from `.doti/payload.json`, single-sourced through the same `DotiVersionRelationCalculator` (GitVersion semver compare) the new commands use, with the scaffold stamp as a fallback only. New `*.Core` types (`RepoPayloadStore`, `DotiVersionRelationCalculator`, `DotiVersionInspector`, `DotiRepoScanner`, `DotiUpdater`, `DotiWorktreeUpdate`, `DotiBatchUpdater`) + a reusable `GitWorktree` primitive in `Hx.Runner.Core`, with thin `hx doti` CLI delegating to them; four new diagnostic codes (`doti-not-a-repo`, `doti-version-unknown`, `git-required`, `doti-update-failed`).
- **Fixed** the `hx new` scaffold template shipped a **`Directory.Build.props` with an XML-invalid comment** — the `020` auto-year-copyright comment contained `--company`, and `--` is illegal inside an XML comment, so every **generated** repo's `Directory.Build.props` failed to parse (`MSB4024`), its `TargetFramework` was never set, and the whole repo failed to build (`NETSDK1013`). This was the root cause of the **v0.13.1 release-CI failure**: the NuGet `publish` job was *skipped* because the pack-and-smoke `hx new` build broke (the package installed fine — only the *scaffolded* repo was broken), so `0.13.1` never reached NuGet. Fix: rephrase the comment to drop the `--`; verified by repack → regenerate → `dotnet build` (0 errors). The golden tests read these assets as **strings**, so the invalid XML stayed green — added `XmlAssetValidator` (+ the `Every_template_xml_asset_is_well_formed_xml` golden test), a reusable well-formedness linter built on the BCL `XmlReader` (the canonical .NET XML linter — the same parser MSBuild loads project files through, no third-party or native dependency) that parses every shipped .NET XML asset, discovered by a domain-complete MSBuild/solution/packaging extension set **plus** an `<?xml`-declaration sniff (not the original hard-coded `.props`/`.csproj`/`.slnx` trio), with its own unit tests proving it flags the `--`-in-comment defect and never false-positives on non-XML. Scope is the raw template; post-`dotnet new` validity stays the round-trip build's job. So an XML-invalid template now fails in the gate, not in a downstream generated build.
- **Changed** the release process to **`dev` → `main` squash-merge**. `dev` is the permanent working branch; `main` is the protected release branch (PR-only, squash-merge, owner-restricted). `release.yml` now triggers on the **merge to `main`** (no longer on a hand-pushed `v*` tag): it computes the **controlled** release version with GitVersion on the merge commit (GitHubFlow/v1 — default **patch**; a `+semver: minor|major` in the squash-merge message opts up), creates the `v<version>` tag, packs + smoke-tests the global tool, publishes to NuGet.org via Trusted Publishing (OIDC), and cuts the GitHub Release — all idempotent on re-run. Also: this repo's `GitVersion.yml` switched to `GitHubFlow/v1` (so it, like a generated repo, defaults to patch and treats `dev` as a pre-release branch, not a GitFlow next-minor `develop`), and the README header gained Heurex-styled status badges.
- **Fixed** `021-transition-freshness-overread` (bug mini-cycle): the doti cycle's prerequisite-FRESHNESS change-set identity over-read a stage's OWN in-flight artifact. The `/08` drift-review report, authored after implement and staged for the `drift-review → release` transition, moved the identity that the diff-kind `implement` proof (bound to an empty diff by the doc-stage lag) is compared against, so `implement` read `stale (code changed since stamp)` and **every release stamp blocked** until a manual re-stamp. Fix: subtract the feature's owned doc/review artifacts (`FeatureArtifactScope.OwnedPaths`) from the freshness identity (a pure `ChangeSetIdentity.Compute(..., excludedPaths)` overload + a `CycleService.FreshnessIdentity` helper used by `Check` + `Status`; the existing `excludedOwnedPaths` is now threaded into the transition's `Check` too). Safe and minimal: the only diff-kind stage (`implement`) produces no artifact, so a code edit is never subtracted (a real post-implement code change still stales `implement`); doc/review artifacts are bound by their own proof hashes, not this identity; the gate-proof identity is untouched. Adds a pure exclusion unit test + two integration tests (the reproduced over-read now resolves to `implement: fresh`; an unowned code edit still resolves to `implement: stale`); 226 existing cycle-enforcement tests unchanged.
- **Added** `020-scaffold-versioning-copyright-defaults`: the `hx new` scaffold template now ships the two generated-repo defaults an operator previously had to hand-add. (1) A `GitVersion.yml` (`workflow: GitHubFlow/v1` + `next-version: 0.1.0`) so a generated repo versions correctly out of the box — the trunk `main` increments **patch** by default (a doti bug-fix-only cycle → patch), a feature cycle still bumps **minor** via the existing `+semver: minor` release-transition signal, and the series **starts at 0.1.0**. This closes a latent defect: with no version config a generated repo fell back to GitVersion's **GitFlow** default, under which a `develop`/`dev` branch is the next-minor integration branch — so a bug-fix cycle there bumped *minor* instead of patch (observed in a generated repo, RCA'd, and confirmed fixed by the `GitHubFlow/v1` config + clearing the stale `.git/gitversion_cache`). (2) An auto-year `<Copyright>` in the template's `Directory.Build.props` (`Copyright © $([System.DateTime]::UtcNow.Year) $(Company)`) so a generated repo's release assembly carries `Copyright © <release-year> <Company>` — the year stamped at release-build time, the holder flowing from `--company`. Golden tests assert both defaults so they cannot silently regress. Template-content + test only; no `*.Core`/`*.Cli` code, no architecture-rule change, and this repo's own assemblies/release are untouched (the template props ship as static, deterministic content).
- **Fixed** `019-release-publish-job-fix`: the `release.yml` NuGet-publish job — the `production`-environment Trusted-Publishing deploy — failed at `setup-dotnet` with `The specified global.json file 'global.json' does not exist`: the job referenced the repo's SDK pin but has no `actions/checkout` (it only runs `dotnet nuget push`). Switched it to `dotnet-version: '10.0.x'`. The OIDC `id-token: write` scoping, the `production` environment, the NuGet login, and the Trusted-Publishing-only credential path are unchanged; the pack-and-smoke job (which does check out + build) keeps `global-json-file`. CI/deploy-config only.
- **Fixed** `018-doti-converge-rename`: renamed the `converge` utility **skill** to `doti-converge` so it groups with the other `/doti-*` utility skills — it was the only one missing the `doti-` prefix, which sorted it away from its siblings and made it hard to find. The `hx doti converge` **command** is unchanged (only the slash-skill name + its rendered assets and doc references changed). Cosmetic / discoverability; re-rendered the skills, removed the obsolete `converge` skill dirs, parity clean.
- **Changed** `017-readme-currency`: refreshed `README.md` to describe what `main` actually ships — the "Proofs, gates, and recovery" intro now spans the **007–016** work (not 007–011), and the CLI-map `gate run` / `sentrux` rows mention the live `--stream` trace and the structural-offender detail. Docs-only; no code, skill, or proof change.
- **Fixed** `016-exec-bit-and-context-refresh`: tool binaries fetched/store-populated by the workflow are now marked **executable on Unix** (`0755`). The v0.12.1 release CI fetched + hash-verified the per-RID Sentrux/Gitleaks from the fork but then hit `"Permission denied"` running them (a downloaded file lands `0644`); the verified binary is now runnable on Linux/macOS (the set is guarded so it is a no-op on Windows). Also **refreshed the doti agent context** — `.doti/agent-context.md` + `CLAUDE.md`/`AGENTS.md`, re-rendered from their template/profile source — to describe the capabilities added since cycle 012: `/doti-auto`, the ArchUnit/Sentrux structural-offender detail, Sentrux's production-only `.sentruxignore` scope, and the exec-bit guarantee, so an agent reading the repo's context sees the current doti. Correctness + docs only; no rule, limit, or proof change.
- **Changed** `015-release-accuracy`: documented the **unnumbered utility skills** in the README — `/doti-constitution` (the two-layer §1 inherited / §2 operator-authored project constitution, re-read fresh at plan + arch-review) given a fuller treatment, plus `/doti-auto`, `/doti-bug`, `/doti-amend`, `/doti-drift-fix`, `/converge`, and `/doti-upgrade`. Also fixed the release CI (`release.yml`) to provision the **host-RID** vendored structural tools (Sentrux + Gitleaks from the pinned `heurexai/*` fork releases, manifest-pinned + hash-verified, fail-closed) into the installed payload before the `hx new` install smoke, so the published global tool's first-smoke verifies them on Linux instead of failing-closed and the NuGet publish proceeds. Docs + CI-config only; no rule, limit, or proof change.
- **Added** `014-structural-violation-detail`: structural-engine violation detail — when the architecture (ArchUnitNET) or Sentrux gate fails, the operator now sees **which files/types caused it**. ArchUnitNET surfaces the violating types + rule description (captured from `rule.Evaluate` through the TRX failure message, no longer discarded by the boolean `HasNoViolations`); Sentrux surfaces a structured offender (rule, file, function, line, measured-value-vs-limit) instead of a flattened string, with honest `unknown`-with-reason for summary-style rules. The detail renders in the standalone `architecture test`/`sentrux check` commands and in the `gate run` ladder (012's render), deterministically ordered and capped with `+N more`. **Load-bearing invariant:** the detail is additive and render-only — it lives on the `GateRunResult.Trace` envelope + the standalone result contracts, never in the hashed `GateProof`, so the persisted gate proof + `gateProofDigest` stay byte-identical (a `ProofHashBoundaryTests` digest-equality proof + an ArchUnit boundary assertion enforce it). **Changed** Sentrux scoped to **production code** — `.sentruxignore` excludes the repo's `test/` tree (a large, well-structured test fixture is not a production "god file"), with the consequent one-time authorized FR-031 baseline raise to the production-only signal (a stricter floor, never a lowering).
- **Added** `013-doti-auto-mode`: a new unnumbered `/doti-auto` utility skill that drives the numbered cycle (`/01`–`/09`) **hands-off** to a target stage (`--until <stage>`, default the local release), advancing stage-to-stage automatically while nothing needs the operator and stopping only at genuine operator-decision points (a clarify ambiguity, an arch-review BLOCKER, an unrecoverable gate failure, the publish boundary). It is orchestration over the enforced stages — never a bypass: it never weakens a gate, skips a stamp, or pushes a release tag. Doti-prose only (single-sourced in `.doti/core/skills.json` + a command template, rendered like the other utility skills).
- **Added** `012-affected-test-plan-visibility`: gate & affected-test telemetry so an operator can see what a doti step is actually doing. `gate run --stream` renders a per-step ladder (outcome + per-step timing + total), a two-tier change summary (basic files/lines every gate; modified classes + the affected-test inventory only at the implement code gate), and the resolved scope/ladder. All telemetry lives on the `GateRunResult.Trace` envelope — never an input to a `*ProofHasher` (008 FR-020 boundary), proven byte-identical.
- **Changed** `011-readme-accuracy-and-constitution`: corrected and refreshed `README.md` — fixed the stale `.doti/core/memory/constitution.md` path (single-sourced in 010), added `hx doti constitution` to the CLI map, and reworked the Spec Kit comparison so it no longer implies doti lacks a constitution: it now contrasts Spec Kit's single SemVer-versioned constitution (with a Sync Impact Report) against doti's two-layer **§1/§2** constitution re-injected fresh into `plan` and `arch-review`. Docs-only.
- **Changed** `010-single-source-constitution`: removed the redundant `.doti/core/memory/constitution.md` twin — the project constitution is now **single-sourced** at `.doti/memory/constitution.md` (the path the tooling reads, and where every doti-installed repo's constitution already lives). No constitution content change; the twin was a vestigial source-layer copy left over from the pre-009 reconcile model (after 009 made the constitution operator-authored and excluded it from the shipped payload, the copy shipped to no one and was read by no code).
- **Added** `009-constitution-stage-and-fresh-context`: a project **constitution** as always-fresh context. A new unnumbered `doti-constitution` skill authors a **two-layer** constitution — **§1** inherited doti invariants (cited, never re-declared or given a placeholder) and **§2** project declarations (domain, tech stack, coding style, security, performance — the only operator-authored content). `hx doti constitution` emits the current §2 and is **codified** into `/03-doti-plan` and `/06-doti-arch-review` (composed into `review-context`) so the agent always reviews against fresh §2 — delivery is code-enforced, evaluation is agent-judged (the deterministic gate is unchanged). The structured `constitution-template.md` ships to `hx new` repos with the generated repo's constitution **initialized from the template** (project name auto-derived) instead of inheriting speckit-doti's; this repo keeps its real 9-principle constitution plus an explicit §2. The advisory, never-gating `hx doti drift-candidates` finder is **.NET-tuned** — lexer-aware C# member chunking, a code-aware embedding instruction, and .NET-calibrated thresholds — documented as an on-demand, code↔docs-only tool with explicit match-type guidance (paraphrase → semantic, IDs → grep, contradictions → reasoning).
- **Fixed** local-release root resolution no longer hard-codes a machine path: `hx.config.json` resolves an explicit directory, then a named env var, then `DOTI_RELEASE_ROOT` (this machine overrides to `HEUREX_RELEASE_ROOT` via a gitignored local config + a portable committed `hx.config.template.json`); the NuGet `PackageOutputPath` in `release.yml` is now an absolute path.
- **Added** `008-doti-review-recovery-and-change-context`: review **recovery** and deterministic **change context** for the Doti cycle. New `hx doti cycle refresh-plan`/`refresh --apply-safe` recovers a stale cycle by re-binding only the safe-to-reinterpret stamps — classifying each stage `SafeReinterpret` / `RerunRequired` / `NotBound` with typed stale reasons and `nextActions` hints — instead of hand-guessing a stamp. A reusable status-rich `ChangeSetContext` (Added/Modified/Deleted/Renamed) drives `hx doti review-context` (arch-review lens applicability as data), `impact plan --for change-context`, and a docs-only gate/Sentrux scope skip (`GateScope`, recomputed at validation).
- **Changed** Sentrux is now source/code scoped (`.sentruxignore` excludes `.md`/docs/Doti prose/rendered skills/payload metadata), gains an escalation-band **two-try** optimization diagnostic, and gates rebaseline behind explicit operator intent **plus** a change-set-fresh arch-review record classifying the growth as functionality-driven — the baseline is **never** removed. Adds cross-feature **release-train drift** detection (a later feature changing an earlier completed-unreleased feature's owned paths), single-source materialized `.doti/templates` (the committed twin is removed; templates are materialized from `.doti/core/templates`), and the unnumbered utility skills `doti-amend` / `doti-drift-fix`.
- **Added** an **advisory, never-gating, fully-offline** semantic drift finder — `hx doti drift-candidates` embeds changed code vs reference prose on a local CPU model (primary **Qwen3-Embedding-0.6B** via GGUF/LLamaSharp, fallback **BGE-M3** via ONNX), pinned + hash-verified before native load, reporting the active engine; an empty candidate list is explicitly not a clean-bill signal. The reusable `Hx.Embedding.Core` library carries zero workflow dependencies, and `Hx.Gate.Core`/`Hx.Cycle.Core` are compile-checked to never depend on the semantic stack.
- **Added** `007-standalone-hx-source-independent-cli`: installed `hx` runs the full workflow **source-free** (`gate run`, `architecture test`, `sentrux verify`/`check`, `hygiene`/`security scan`, `version calculate`, `doti cycle`/`render-skills`/`install`/`payload check`/`install-hooks`, and `impact plan` — FR-045), resolved from a payload beside the executable (`PayloadRoot`, `HX_PAYLOAD_ROOT`). Two distribution channels — the **NuGet .NET global tool** `Heurex.SpeckitDoti` and the **Microsoft Store MSIX** — three install tiers (`workflow-only` / `dotnet-lib` / `dotnet-cli-heurex`) that scale the gate ladder to how much governance a repo adopts, and absorbed utilities: the enforced bug mini-cycle (`hx doti bug`), `hx doti converge` (spec↔tasks drift), the "unit tests for English" requirement checklist, and machine-readable arch-review changed-files context (`impact plan --for arch-review`).
- **Removed** Velopack and `vpk` entirely (superseding 006's Velopack-only release output): no installer/update packages, no `vpk pack`, and scaffolded apps carry no Velopack stub. Installed `hx` updates via its channel (`dotnet tool update -g Heurex.SpeckitDoti`, or the Microsoft Store); `hx doti install` remains the repo workflow-asset install/repair path.
- **Changed** `hx release` now packs the `Heurex.SpeckitDoti` global tool (`dotnet pack`) with a source-free install smoke and records the Microsoft Store MSIX channel proof, instead of staging Velopack artifacts. The default version intent is cycle-type-aware (feature → `minor`, bug-fix-only → `patch`); a pushed `v*` tag drives NuGet Trusted Publishing (`release.yml`) and the Store workflow (`store-release.yml`).
- **Added** `006-task-hash-gated-velopack-completion`: task-hash-gated implementation proof, multi-feature release-train handling, Velopack-only installer/update release artifacts, scaffold payload parity proof, executable-local `hx.config.json`, generated starter configuration, release documentation proof, and stable diagnostics for the new fail-closed surfaces.
- **Changed** `hx release` now owns release intent and local tag creation (`--major|--minor|--patch`) and records tag/GitVersion/Velopack payload identity in the local release result.
- **Removed** the bespoke repository updater command and the runner-side standalone tag command; Velopack owns installed executable updates, while installed `hx doti install` is the released repo workflow asset install/repair path. The source-runner `doti install` path remains developer-only.
- **Added released `hx doti install`** so installed `hx` can install, repair, or migrate repo-owned Doti workflow assets from the `.doti` payload beside `hx.exe`, with explicit `--repo`, target classification proof, hook arming, and no source checkout or `dotnet run` requirement.

## [0.4.0] - 2026-06-23

- **Added** trusted prerequisite preflight: `hx prereq check` reports .NET SDK, Git, directory readiness, and manifest identity; `hx prereq install` runs only a release-defined, digest-approved Windows winget plan.
- **Changed** `hx new` and `hx update` now run prereq/directory preflight before side effects, generated repos carry `.doti/prerequisites.json`, and `hx version --repo` reports read-only prereq health.
- **Fixed** standalone archives can run `hx new` from the extracted payload without a `.git` checkout; the template pack no longer imports GitVersion, and scaffold failures include stage evidence.

## [0.3.1] - 2026-06-23

### Added
- **Existing-repo update path** — `hx version` and `hx update` now report the running toolkit, target repo scaffold stamp, and exact managed template/skill modification state; update resolves the latest GitHub Release asset, verifies checksums, uses a temporary cache, creates a backup worktree by default, and preserves live repo configuration such as Sentrux baselines.
- **Cycle recovery hardening** — completed doti cycles now recover cleanly from post-commit completion-state failures, repeated completion is idempotent, and `doti cycle commit` re-verifies the gate proof, affected-test hashes, staged tree, and Doti trailers before accepting a commit.

### Changed
- **Version stamping is GitVersion-backed and release-clean** — all scaffold CLIs now report the public product version only (`0.3.1` for this release), without branch names, commit SHA, prerelease height, or build metadata.
- **Release and Store packaging are version-guarded** — GitHub Release and Store workflows fetch full tag history and fail if the packaged `hx` version does not exactly match the release/MSIX version.
- **Human help is fully shared** — root commands, command groups, and leaf-command help use the same rich/plain renderer and support ANSI-free output through `--help-mode plain`, `--plain-help`, `HX_HELP_MODE=plain`, and `NO_COLOR`.

### Fixed
- Release hygiene now scans the working tree instead of sticky git history for full-scope checks and excludes generated release/package artifacts.
- Public docs and packaging notes avoid local-machine path markers that can trip hygiene or secret-scan heuristics.

## [0.3.0] - 2026-06-23

### Added
- **Heurex-branded CLI experience (Spectre.Console)** — human/TTY output now renders a branded figlet banner + a rounded command table for help, and `hx new` shows **live per-step progress bars** with a final summary panel instead of a long silent pause followed by a single line. Navy/gold Heurex palette throughout.

### Changed
- All human rendering flows through **Spectre.Console in `Hx.Cli.Kernel`** (the single human-output site) — no thin-CLI / channel-independence rule violation. The scaffold runners (`ScaffoldNewRunner`, `FirstSmokeRunner`) emit a `CliEvent` per step through an optional progress sink, so a channel can render progress without the core depending on the renderer.
- The agent-first JSON envelope is **byte-identical** in `--json`/piped mode (the progress callback is a no-op there), so the machine contract is unchanged. `Spectre.Console` is pinned in both `Directory.Packages.props` files so the vendored kernel restores in generated products too.

## [0.2.0] - 2026-06-21

### Added
- **Cross-platform installers** — the release builds a standalone installer for **win-x64** (`.zip`), **linux-x64**, and **osx-arm64** (`.tar.gz`) on native runners, each attached to the GitHub Release. The vendored tool manifests gained linux-x64 + osx-arm64 assets (the Heurex sentrux fork ships every platform), and `ToolFetcher` now extracts `.tar.gz`, not just `.zip`.
- **Per-RID sentrux grammars** vendored in-repo (linux `.so` + macOS `.dylib` alongside the win `.dll`); the store, resolvers, and grammar stager were already RID-keyed.
- **Package-manager templates** — a winget manifest + Homebrew formula + `packaging/PUBLISHING.md` (published post-release, once the archive hashes exist).

### Note
- Archives and `hx` remain unsigned; macOS notarization + Windows Authenticode are deferred until certificates are available.

## [0.1.0] - 2026-06-21

First tagged release — the toolkit is published as a downloadable, standalone win-x64 installer.

### Added
- **Standalone installer** — a self-contained `hx.exe` plus the bundled scaffold payload (template, doti workflow, source projects, and the vendored tool binaries), published as a GitHub Release asset. `hx new` scaffolds a project from any directory (the payload is resolved relative to the executable).
- **Shared tool store** — the vendored binaries (Gitleaks, Sentrux, GitVersion) install once into a per-user data folder with an environment override; generated solutions resolve them store-first (in-repo fallback) instead of carrying a ~127 MB per-solution copy.
- **Thin-CLI architecture enforcement** — a *Channel Independence (Thin Adapter)* constitution principle, plan/arch-review channel-boundary checks, and two new ArchUnit families (`cliSurfaceConfinement`, `cliDelegation`) shipped in the generated template (nine families total) and dog-fooded on the toolkit's own `Hx.*.Cli` projects.
- `tools fetch` — deterministic, hash-verified provisioning of the vendored tool binaries from their pinned manifests (fail-closed on mismatch).
- Agent-first CLI self-description (structured `<PREFIX><NNNN>` error codes, `describe`, the `CliResult` envelope) encoded into the doti workflow.

[Unreleased]: https://github.com/heurexai/speckit-doti/compare/v0.4.0...HEAD
[0.4.0]: https://github.com/heurexai/speckit-doti/compare/v0.3.1...v0.4.0
[0.3.1]: https://github.com/heurexai/speckit-doti/compare/v0.3.0...v0.3.1
[0.3.0]: https://github.com/heurexai/speckit-doti/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/heurexai/speckit-doti/releases/tag/v0.2.0
[0.1.0]: https://github.com/heurexai/speckit-doti/releases/tag/v0.1.0
