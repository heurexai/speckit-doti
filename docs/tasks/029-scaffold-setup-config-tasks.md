# 029 — Tasks: scaffold & doti setup configuration

**Priority Mode:** tooling/CLI — fail-closed safety + deterministic proof before ergonomics. P1 = the agent-facing deterministic core (config model + `--config` + `config show`); P2 = the human wizard; P3 = project-file projections (folded into P1's projector); P4 = the operator checklist + docs. Each task maps to its FR/SC for `/06-analyze` coverage. Module homes per the revised plan (Contracts = pure model/resolver/schema/projector; Doti.Core = `.doti`-asset writers/store/formatter; Scaffold.Core = project writers; CLI = thin parse→delegate→render, wizard named neutrally).

## Phase 1 — P1 deterministic core (US1)

- [x] T001 [P1] `Hx.Tooling.Contracts`: add `SetupConfig` (nullable DTO), `ResolvedSetupConfig`, `ConfigField{Value,Source,Default}`, `SetupKey` (with `Group` ∈ identity|versioning|release|publish|agents|constitution and `AppliesTo` ∈ New|Install|Both), `ConfigSource` enum, and `SetupConfigDefaults` (the single defaults source). (FR-001, D1) <!-- doti-task-hash: 6422da945fed4d2445f7dddd6eb76cb1753551848a199fb4b0be861a93045349 -->
- [x] T002 [P1] `Hx.Tooling.Contracts`: `SetupConfigSchema.Validate` — fail-closed on `schemaVersion≠1`, unknown fields, `agents ⊄ {claude,codex}`; a shared 3-part-numeric-SemVer parser for `nextVersion`; SPDX charset for `license`; reject control chars + XML metacharacters in free-text; path-containment helper (`..`/rooted checks). Each error names the offending field. (FR-009, D5, D9) <!-- doti-task-hash: 5fa3e4dddb0407927795f9761bb89fb31ee5755a134d2cadf5a942386f2ce30c -->
- [x] T003 [P1] `Hx.Tooling.Contracts`: `SetupConfigResolver.Resolve(raw, flagOverrides, audience)` → `ResolvedSetupConfig`, layering flag ▸ config-file ▸ interactive ▸ derived ▸ default and recording the winning `Source` per key. (FR-001, FR-002, D2) <!-- doti-task-hash: 9407e0aa62993c602e0481e498b307fbff38f3d12f52ce3e52740513c67fe65e -->
- [x] T004 [P1] `Hx.Tooling.Contracts`: `ISetupTargetWriter` + `SetupConfigProjector.Project(resolved, audience, writers)` — iterate the single key→target table, audience-scoped; collect+report `New`-only keys reached on `Install` as ignored; **provable no-op when `Setup` is null/absent (zero writer calls)**. (FR-002, FR-006, D3, D10) <!-- doti-task-hash: a9e5501d5f3274da4afd0ec9ee228f8ecc5195a46276ea738bc82bc71f4e0e24 -->
- [x] T005 [P1] `Hx.Doti.Core`: the `.doti`-asset writers as `ISetupTargetWriter`s — `ReleaseTargetWriter` (re-home/duplicate from Scaffold.Core), `GitVersionSeedWriter`, `CsprojMetadataWriter` (XML-encode every value via `SecurityElement.Escape`/`XElement`), `ConstitutionSection2Writer` (beside `ConstitutionInitializer`, reuse `ConstitutionService.Section2Anchor`, write-once-when-placeholder, anchor-integrity, idempotent). (FR-006, D3, D9) <!-- doti-task-hash: 29d01343099e6b5300a67844dfcbae6efacabe10c997f660c2c0f0e6193263dc -->
- [x] T006 [P1] `Hx.Doti.Core`: `SetupConfigStore` (read/write `.doti/setup.json` — persist repo-portable intent only; never write machine-local absolute paths e.g. `localOutput.directory`) + `SetupConfigTableFormatter` (human table grouped by `Group`, `N custom · M default` footer). (FR-003, FR-004, D6, D7) <!-- doti-task-hash: bed38d29b4e1b96317eea356a6262c8c1dadeae03e8a4f3972576b243487a851 -->
- [x] T007 [P1] `Hx.Tooling.Contracts`: add additive optional `ResolvedSetupConfig? Setup = null` to `ScaffoldRequest` and `DotiInstallBootstrapRequest`; confirm `ScaffoldProof.Request.Setup` serializes additively (null/omitted on no-config). (FR-008, D8) <!-- doti-task-hash: 4b1a4f588b27cf85394afc3b11cd121c7a3e9a0948c20c9c2f18171a7d489562 -->
- [x] T008 [P1] `Hx.Scaffold.Cli`: `--config <path>` on `hx new` — read + `SetupConfigSchema.Validate` + resolve **before** building `ScaffoldRequest` (so an invalid config never reaches `ScaffoldNewRunner`); flags override config; contain `identity.output` + the `--config` path. (FR-002, FR-008, D5, D9) <!-- doti-task-hash: 913a9662d2de1221314d023b2752a9fb16a4bfe5aba5ef3590d0e8c42d6bd115 -->
- [x] T009 [P1] `Hx.Scaffold.Core`: projection step in `ScaffoldNewRunner` after `TemplateGenerator` — supply the Scaffold.Core project writers + the Doti.Core `.doti`-asset writers to `SetupConfigProjector`. (FR-002, FR-006, D3) <!-- doti-task-hash: 9d50dd9a0200d1b640790ded15cd34f8914093901f1bfb5c92a1860aa91c27e3 -->
- [x] T010 [P1] `Hx.Runner.Cli`: `hx doti config show [--json]` — resolve via the Doti.Core store + Contracts resolver; render JSON (`{value,source,default}`) or the human table; **all-default view when `.doti/setup.json` is absent** (not an error); non-mutating. (FR-004, D7) <!-- doti-task-hash: 4122e3c16106a62cc12f38f4d2bc6d2d43121cf4246661929d20fc3d4f7dde0d -->
- [x] T011 [P1] Regression lock: `hx new` with neither `--config` nor `--interactive` is byte-identical to today (files + the `--json` proof shape). (FR-008, SC-007, D10) <!-- doti-task-hash: edd854a42ae128fb5a30b272c759118d9488acfc0418183cc0662520b436b835 -->

## Phase 2 — install path (US1)

- [x] T012 [P1] `Hx.Runner.Cli`: `--config <path>` on `hx doti install` — validate + resolve (audience = Install) + carry on `DotiInstallBootstrapRequest.Setup`. (FR-002, FR-010, C3, D8) <!-- doti-task-hash: 5df3545afbee0aa118486836aa2f8e263b4160f7cf1cbfc9bf98ecb5d2a8b4df -->
- [x] T013 [P1] `Hx.Doti.Core`: `DotiInstaller` runs the Install-subset projection (Doti.Core writers) + reports `New`-only fields as ignored; provable no-op when `Setup` absent; **`hx doti install` no-config byte-identical** regression (also guards `doti update`/`update-all`). (FR-002, SC-005, SC-007, D8, D10) <!-- doti-task-hash: 07e6594b12e1a86a0c8bdd31a063fb0f95575f4650e066a18633826bb4ffdfb8 -->

## Phase 3 — P2 interactive wizard (US2)

- [x] T014 [P2] `Hx.Tooling.Contracts`: `SetupPromptDefinitions` — pure data per key (question, default, validation ref, `Group`, `AppliesTo`, `EnabledWhen` for conditional branches, the "what it affects" string). (FR-005, D4) <!-- doti-task-hash: 7b5c1884f89a5f83c1310db0d303c669d5377832309c7565d700ec8701674b08 -->
- [x] T015 [P2] CLI: the wizard IO loop (neutral name e.g. `SetupWizard`/`SetupPrompts` — no forbidden `cliSurfaceConfinement` suffix) behind an injectable `IConsole`/reader; iterate `SetupPromptDefinitions`, collect a `SetupConfig`, re-enter the `--config` path; refuse when `--config` is also passed; end with a Review step rendering `config show` (human) before any write. On `hx new` and `hx doti install` (install asks the applicable subset). (FR-005, D4) <!-- doti-task-hash: 2460d81f55992dd532fba2f8fa1bb99136bb05768a32a09ba60c0f685294ac3b -->
- [x] T016 [P2] Test: scripted-input wizard run produces the same repo + `.doti/setup.json` as the equivalent `--config` file. (FR-005, SC-004) <!-- doti-task-hash: 9992024be4c337e6462e39d7a18e52830a1badc50e01d89c4673ea349091e304 -->

## Phase 4 — operator checklist + docs (US4 reduced)

- [x] T017 [P4] CLI: print the operator-intent **checklist** (FR-007, never executed) naming the operator-only steps (NuGet OIDC policy, `NUGET_USER` secret, `production` Environment, `main` branch protection, `v*` tag push) **plus** the git/CI steps deferred to 030 (baseline commit, `dev`/`main`, `.github/workflows`, DCO hook). (FR-007) <!-- doti-task-hash: fef62e26b55475322d4ac5c1aaf3d1a4f7dce9e922f1c1968676a7c119664f77 -->
- [x] T018 [P4] Docs: update `docs/configuration.md` + the README Configuration section "How" column (the projected fields move from "edit `.csproj`/file" to "`--config`/wizard"); add a `doti-setup.json` example. (FR docs) <!-- doti-task-hash: 70ba8063e1ebe5b76fc06d47f68546701e3c0d849b74db3b09a0e395aa12825c -->

## Phase 5 — verification + release docs

- [x] T019 [P1] Integration + golden suite: SC-001..SC-008; `description="A & B"` → valid `.csproj` (D9); `--output ../x` rejected (D9); `config show` on a no-config repo → all-default (D7); machine-local `localOutput.directory` never in tracked `.doti/setup.json` (D6); a `doti-setup.json` fixture + expected `config show --json`; assert `SetupConfigDefaults == docs/configuration.md`. (SC-001..008) <!-- doti-task-hash: d11fdfbb19ac8a151a8649215b743e82da328c692e76bebeb94b1ae8d3180e95 -->
- [x] T020 [P4] `CHANGELOG.md` + `README.md`: add the `029-scaffold-setup-config` feature note (release-train documentation). (FR docs) <!-- doti-task-hash: 03a7f99779204e61de48d6241baf127ddb5d50b2e8430a497a3d29f596d00fd4 -->
- [x] T021 [P4] **Documentation sweep (permanent final task):** verify the agent-context, rendered skills, `hx describe`/`--help`, README, and `docs/configuration.md` all describe `--config`/`--interactive`/`config show` consistently; `doti render-skills --check` + `doti payload check` green. (FR docs) <!-- doti-task-hash: c53b77b5566717bc92902fddc23551763cda44b8e9605819055983e604524ce6 -->
